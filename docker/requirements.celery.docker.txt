# Docker Linux requirements for Celery worker that may run AI workloads.
# Start from web runtime set, then add AI stack.

-r requirements.web.docker.txt

imutils==0.5.4
img2pdf==0.5.1
timm==1.0.22
einops==0.2.0
